# ================================================================================
# CHAPTER 12: Environment Variables - Google OAuth
# ================================================================================
# Copy this file to .env and fill in your values.
# IMPORTANT: Never commit .env to version control!
# ================================================================================

# ================================================================================
# GOOGLE OAUTH CONFIGURATION
# ================================================================================
# Get these from https://console.cloud.google.com/
#
# Setup Steps:
#   1. Go to Google Cloud Console
#   2. Create a new project (or select existing)
#   3. Go to "APIs & Services" > "OAuth consent screen"
#      - Choose "External" user type
#      - Fill in app name, support email
#      - Add scopes: email, profile, openid
#   4. Go to "APIs & Services" > "Credentials"
#   5. Click "Create Credentials" > "OAuth client ID"
#   6. Select "Web application"
#   7. Add authorized redirect URI:
#      http://localhost:5012/auth/google/callback
#   8. Copy the Client ID and Client Secret below
# ================================================================================

GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# ================================================================================
# APPLICATION CONFIGURATION
# ================================================================================

# Secret key for JWT tokens
SECRET_KEY=your-super-secret-key-change-this

# Secret key for Flask session (used for OAuth state)
FLASK_SECRET_KEY=another-secret-key-for-sessions

# Base URL for OAuth callbacks
BASE_URL=http://localhost:5012

# ================================================================================
# IMPORTANT NOTES
# ================================================================================
#
# 1. REDIRECT URI MUST MATCH:
#    The redirect URI in Google Console MUST exactly match:
#    http://localhost:5012/auth/google/callback
#
# 2. OAUTH CONSENT SCREEN:
#    While in "Testing" mode, you need to add test users.
#    Go to OAuth consent screen > Test users > Add users
#
# 3. PRODUCTION:
#    - Use HTTPS (required by Google for production)
#    - Submit app for verification to remove "unverified app" warning
#    - Update redirect URIs to production domain
#
# ================================================================================
